[project]
name = "hiccup"
version = "0.2.0"
description = "Utilitário para automatizar controle do mouse no sistema"
authors = [
    { name = "Caio Campos", email = "20524962+caiocampos@users.noreply.github.com" }
]
requires-python = ">= 3.12"
dependencies = [
    "evdev>=1.9.3",
]

[project.scripts]
"hiccup" = "hiccup:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hiccup"]
